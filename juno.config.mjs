import { defineConfig } from "@junobuild/config";

export default defineConfig({
  satellite: {
    id: "gwank-kqaaa-aaaal-ar2cq-cai", // Replace with your satellite ID
    source: "build" // Replace with your build output directory
  }
});
